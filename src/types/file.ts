import { TAllowedFileTypes, ALLOWED_FILE_EXTENSIONS, MAX_FILE_SIZE } from './shared';

// Re-export shared file-related types and constants
export type { TAllowedFileTypes };
export { ALLOWED_FILE_EXTENSIONS, MAX_FILE_SIZE }; 